diff --git a/ateliers/atelier-02-rag-simple/solution.py b/ateliers/atelier-02-rag-simple/solution.py
index cb1c397..cea42b9 100644
--- a/ateliers/atelier-02-rag-simple/solution.py
+++ b/ateliers/atelier-02-rag-simple/solution.py
@@ -69,8 +69,8 @@ def main() -> None:
     # Semantic (SemanticChunker)             → coupe sur rupture de cohérence
     # Recommandé par défaut : recursive.
     # ═══════════════════════════════════════════════════════════════════
-    chunks_fixed = chunk_fixed_size(pages, chunk_size=512, chunk_overlap=50)
-    chunks_rec   = chunk_recursive(pages, chunk_size=512, chunk_overlap=50)
+    chunks_fixed = chunk_fixed_size(pages, chunk_size=2000, chunk_overlap=50)
+    chunks_rec   = chunk_recursive(pages, chunk_size=2000, chunk_overlap=50)
     print(f"  chunks_fixed     : {len(chunks_fixed)}")
     print(f"  chunks_recursive : {len(chunks_rec)}")
 
